images:
  app3:
    "1.2.3":
      registry: registry.stevedore.test
      namespace: stable
      version: "{{ .Version }}"
      builder:
        driver: docker
        options:
          context:
            - path: app3
      parents:
        busybox:
          - latest
