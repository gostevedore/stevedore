
images:
  scratch:
    latest:
      version: "{{ .Version }}"
      builder:
        options:
      persistent_vars:
        image_from: "{{ .Name }}"
      persistent_labels:
        created_at: "{{ .DateRFC3339Nano }}"

  busybox:
    latest:
      registry: docker-hub.stevedore.test:5000
      namespace: library
      builder:
        options:
      persistent_vars:
        image_from: "docker-hub.stevedore.test:5000/library/{{ .Name }}:{{ .Version }}"
      persistent_labels:
        created_at: "{{ .DateRFC3339Nano }}"

  alpine:
    "3.16":
      registry: docker-hub.stevedore.test:5000
      namespace: library
      builder:
        options:
      persistent_vars:
        image_from: "docker-hub.stevedore.test:5000/library/{{ .Name }}:{{ .Version }}"
      persistent_labels:
        created_at: "{{ .DateRFC3339Nano }}"

  ubuntu:
    latest:
      version: "{{ .Version }}"
      registry: docker-hub.stevedore.test:5000
      namespace: library
      builder:
        options:
      persistent_vars:
        image_from: "docker-hub.stevedore.test:5000/library/{{ .Name }}:{{ .Version }}"
      persistent_labels:
        created_at: "{{ .DateRFC3339Nano }}"
    "20.04":
      version: "{{ .Version }}"
      registry: docker-hub.stevedore.test:5000
      namespace: library
      builder:
        options:
      persistent_vars:
        image_from: "docker-hub.stevedore.test:5000/library/{{ .Name }}:{{ .Version }}"
      persistent_labels:
        created_at: "{{ .DateRFC3339Nano }}"